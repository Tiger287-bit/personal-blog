# ROS 2 与 App 通讯：BNO086 IMU 示例源码

本目录是博客使用的源码归档布局，不是开发板上的直接运行根目录。

- `app/` 对应 `/home/arduino/ArduinoApps/bno086-ros-live`；
- `ros2/ros_ws_bridge/` 是 ROS 2 与 WebSocket 的通用桥接包；
- `ros2/imu_live/` 保存 IMU 路由配置和只读观察器。

归档保留 MCU 库、第三方驱动、Brick、配置、示例、资源索引和许可证，不包含缓存与构建产物。
